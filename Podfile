# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Number Block' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Number Block
    	pod 'Firebase/Core'
  	pod 'Firebase/AdMob'
	pod 'FacebookCore'
	pod 'FacebookLogin'
	pod 'FacebookShare'

  target 'Number BlockTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Number BlockUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
